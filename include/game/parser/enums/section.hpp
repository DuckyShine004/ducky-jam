#pragma once

namespace game::parser::enums {

enum class Section {
    Default,
    General,
    Metadata,
    Difficulty,
    Events,
    TimingPoints,
    HitObjects
};

} // namespace game::parser::enums
