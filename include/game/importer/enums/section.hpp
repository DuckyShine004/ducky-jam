#pragma once

namespace game::importer::enums {

enum class Section {
    Default,
    General,
    Metadata,
    Difficulty,
    Events,
    TimingPoints,
    HitObjects
};

} // namespace game::importer::enums
