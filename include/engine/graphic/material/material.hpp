#pragma once

namespace engine::graphic::material {

/* PWN: Yes, I know that material in this domain is more about lighting, but deal with it */
class Material {
    int texture_id;
    int shader_id;
};

} // namespace engine::graphic::material
